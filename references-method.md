# 大模型计算流程图 —— 绘制方法总结（DSH 可视化模板）

> 目标：用一张**自包含的 HTML（内嵌 SVG，JS 数据驱动）** 画任意大模型的**前向计算/结构流程图**（整模型 或 其中某个子模块），双击即可在浏览器里查看、缩放、复用。
> 本文说明「怎么画」，对应文件：`ds_plan/大模型计算流程图模板.html`（通用模板，可直接作为模板改造）。

---

## 1. 一句话思路

把图拆成两半：
- **渲染引擎（写一次就不动）**：负责排版、画框、画箭头、画虚线重复框、画右上角图例。
- **模型配置（每次只改这里）**：一个 `nodes` 数组 + 一份图例，描述具体模型的模块与 shape。

画一张新图，本质就是**重填 `nodes` 数组 + 更新图例**，排版自动完成。这也是为什么它能批量复用。

---

## 2. 视觉规范（已经跟你确认过的样式）

| 要素 | 规范 |
|---|---|
| 背景 | 纯白 |
| 模块框 | 白色底 + **黑色描边**圆角框 |
| 框内文本 | 第一行 = 模块名（粗体）；其后 = shape 变换流（等宽字体，`a → b`） |
| 箭头 | 框与框之间**居中**的“线轴 + 箭头头”短竖箭头，从上一框底边指向下一框顶边；**箭头尖端必须紧贴（顶到）下方框的顶边，不留空隙**。虚线重复容器内部的子模块之间也必须沿用同样的居中箭头；且**间距要足够大**（否则箭头头会挤成孤立小三角、看起来没对齐） |
| 重复层 | **虚线框**包裹子模块，右上角放「×具体数字」黑底徽标 |
| 图例 | **右上角**固定，说明字母与具体数字的含义 |

### 关于 shape 写字母还是写数字（重要原则）
- **不定值（会随输入变化）→ 保留字母**：如 batch size（`N`）、序列长度（`T`/`S`）。
- **模型确定的维度 → 图例里给出“具体数字 + 含义”**：如 `4096 = 隐藏维度`、`64 = 路由专家数`、`4 = top_k`。
- 同一字母可能有两层含义，要分开标注：例如 `N` 作 batch size（shape 里）与“重复层数”（`×32`）是两回事。

---

## 3. 画图前先确认：要展开到什么程度

计算流程图具体展开多细是不确定的，**展开得越深，需要收集的信息也不同**。所以动工前先跟用户确认：画哪个范围（整模型 / 某子模块 / 某一段到算子级）、要细到什么程度（模块 / 层 / 算子 / 到 head、d_head、top_k 级）；据此再按需收集信息，范围定了要哪些自然就定了。

确定后按下面几步走：
1. 弄清要画的这一段由什么构成（decoder-only / MoE / 多模态 / 有没有可重复的层）。
2. 收集关键维度（展开越深越细，按需取用）：模型级 `hidden_size / num_layers / n_experts / top_k / vocab` 等；细化到算子时再加 `n_head / d_head / d_rep / intermediate_size` 等。
3. 拆成“主流程 + 重复层”，重复 N 次（或对每个 head / 序列重复）的部分用 `repeat` 容器。
4. 为每个框填 in/out shape（整模型 `[N, T, d]` 级别；越深越细到 `[n_head, d_head]`、`[T, n_kv_head, d_rep]`）。
5. 确定标注（字母 vs 数字，图例只列本图用到的），然后渲染 + 校验（箭头居中、坐标无 `NaN`、文字不溢出、箭头紧贴框）。

---

## 4. 对应的代码模板（`大模型计算流程图模板.html` 说明）

### 4.1 关键常量
| 常量 | 含义 |
|---|---|
| `CANVAS_W` | SVG 画布宽 |
| `CX` | 内容列中心（所有框/箭头以此居中） |
| `BOXW` | 外层框宽 |
| `ARROW` | 框间箭头区高度 |
| `LINE_T / LINE_S` | 标题行、shape 行高 |
| `PAD` | 框内上下边距 |
| `CONT_W / SUB_W` | 重复容器宽、容器内子框宽 |

### 4.2 `nodes` 数组字段（核心配置）
| 字段 | 说明 | 示例 |
|---|---|---|
| `title` | 模块名 | `"多模态编码器 + 投影层"` |
| `shapes` | shape 变换流数组（每项一行，等宽字体） | `["[N, T, 3072] → [N, T, 4096]"]` |
| `note` | 可选的小字说明 | `"64 路由专家 · top-4 · 共享 1"` |
| `type:"repeat"` | 标记为“重复容器” | `type:"repeat"` |
| `badge` | 容器右上角的重复次数徽标 | `"×32"` |
| `subs` | 容器内子模块数组，每项同样有 `title/shapes/note` | `[{title,shapes}, …]` |
| `footnote` | 容器底部脚注 | `"深层采用稀疏注意力…"` |

### 4.3 渲染函数
- `rrect()`：圆角矩形（支持虚线 `dash`）。
- `line()`：居中的一行文本（支持 `mono`、`anchor`、`weight`、`color`）。
- `arrow()`：居中竖直箭头（尖端落在目标点，用 SVG marker）。
- `drawNode()`：画普通黑框模块，返回框底部 y。
- `drawRepeat()`：画虚线重复容器 + 右上角徽标 + 内部子模块串。

### 4.4 改图的最小操作
改 `nodes` 数组 + 改右上角图例的 HTML（`.legend` 块），其余不用动。排版与箭头由渲染函数自动完成、全列居中。

---

## 5. 常见问题与注意点

- **正确性 vs 示意性**：作为示意图，架构/维度可以是近似值。图上与图例行里都标注“具体数值随配置不同而有差异”作免责；要用于正式文档时，从官方 `config.json` 取精确值。
- **网络被沙箱禁止时的取数**：环境里 `Invoke-WebRequest`/`curl` 出站被挡，可改用 `web_search` 抓取搜索结果里的关键配置（如某个模型“总参/激活参数/MoE/上下文长度”等），再结合已知架构假设补齐，并注明是近似值。
- **文本溢出**：shape 行偏长时，拆成多行数组（`shapes: [line1, line2]`），或减小字号。
- **对齐校验**：脚本化验证——所有折线/箭头 x 坐标应等于 `CX`，坐标中不得出现 `NaN`。同时确认箭头是“线轴 + 箭头头”的完整样式（不是孤立小三角），且重复容器内子模块间的箭头与主流程一致。实践中：子模块间隙（`subGap`）要远大于箭头头尺寸，建议 ≥ 30px，否则箭头头会占满间隙、看起来错位。
- **箭头紧贴框（重要）**：箭头尖端要正好落到下方框的**顶边**，不要留空隙。实现上让箭头在线段的“终点”结束（终点=下一框顶边），并利用**绘制顺序**——框在箭头之后绘制，框的描边会盖住末端最后一像素，从而做到视觉贴合。用脚本判断：箭头终点的 y（`y2`）应等于对应框顶边的 y。
- **N 的一词多义**：同屏出现“batch size 的 N”和“层数的 N”时要区分，避免与用户“保留字母/换成具体数字”的原则冲突。

---

## 6. 提供的文件

- 方法文档：`ds_plan/LLM计算流程图绘制方法.md`（即本文件）
- 模板示例：`ds_plan/大模型计算流程图模板.html`（通用 decoder-only MoE 结构示意，可直接打开、可改造）

> 改造后想画别的模型/模块："画 XX 模型 / XX 子模块" 交给本 agent，我会按此模板填 `nodes` + 图例输出新的 HTML。

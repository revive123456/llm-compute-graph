---
name: llm-compute-graph
description: >-
  生成大模型计算流程图（自包含 HTML，内嵌 SVG，JS 数据驱动，双击即可在浏览器查看、缩放、复用）：
  用可复用模板填 nodes 数组 + 右上角图例，自动排版画出任意大模型的前向计算/结构流程图（整模型或某个子模块）。
whenToUse: >-
  用户要「画/生成 LLM 大模型（或其子模块）的计算流程图 / 架构图 / 结构示意图 / 前向计算图」，
  或要在文档、博客、汇报里放一张大模型前向计算流程示意时使用。也适用于把某种架构（decoder-only / MoE / 多模态 / 某种算子级展开）可视化。
metadata:
  resources:
    - template.html
---

# 大模型计算流程图绘制（llm-compute-graph）

把任意大模型（或其子模块）画成一张**自包含的 HTML**：内嵌 SVG、JS 数据驱动，双击即可在浏览器查看、缩放、复用。画一张新图，本质就是**复制模板 → 重填 `nodes` 数组 + 更新图例**，排版与箭头自动完成。

## 0. 先做（重要）
- **读取模板**：先用文本读取能力（read）打开本技能目录下的 [`template.html`](template.html)，里面的 `nodes` 数组、常量（`CX/BOXW/ARROW/CONT_W/SUB_W/...`）与渲染函数（`rrect/line/arrow/drawNode/drawRepeat`）就是你填空用的骨架。模板是通用 decoder-only MoE 结构示意，直接可作为改造基座。
- 所有相对路径都以**本技能目录**为基准解析后再使用。

**关键常量**（`template.html` 顶部）：
| 常量 | 含义 |
|---|---|
| `CANVAS_W` | SVG 画布宽 |
| `CX` | 内容列中心（所有框/箭头以此居中） |
| `BOXW` | 外层框宽 |
| `ARROW` | 框间箭头区高度 |
| `LINE_T / LINE_S` | 标题行、shape 行高 |
| `PAD` | 框内上下边距 |
| `CONT_W / SUB_W` | 重复容器宽、容器内子框宽 |

**渲染函数**：`rrect()` 圆角矩形（支持虚线 `dash`）；`line()` 居中文本（支持 `mono`/`anchor`/`weight`/`color`）；`arrow()` 居中竖直箭头（SVG marker，尖端落在目标点）；`drawNode()` 画普通黑框模块、返回底部 y；`drawContainer()` 画虚线重复容器 + 右上角徽标 + 内部子模块串；`drawBranch()` 画**多模态并行分支**（左右两列框 + 底部汇合线）；**`wrapText()`** 做文本宽度估算 + 自动换行（`shape`/`note`/`footnote` 超过框宽自动折成多行，防止长文本溢出框外 / 硬塞单行）。

## 1. 画图前先确认：展开到什么程度
计算流程图的多细是不确定的，**展开得越深，需要收集的信息越多**。动工前先跟用户敲定两件事：
1. **范围**：整模型 / 某子模块（如 Attention、MoE FFN）/ 某一段到算子级。
2. **细度**：模块 / 层 / 算子 / 到 `head`、`d_head`、`top_k` 级。

范围定了要采集哪些维度自然就定了：
- 模型级：`hidden_size / num_layers / n_experts / top_k / vocab`、decoder-only / MoE / 多模态 / 有无可重复层。
- 细化到算子时再加：`n_head / d_head / d_rep / intermediate_size / num_kv_head` 等。
> 若网络出站被沙箱限制（取不到官方 config.json），用 `web_search` 抓搜索结果里的关键配置（总参/激活参/MoE/上下文长度），并**注明是近似值**。

## 2. 布局与视觉规范（必须遵守）
| 要素 | 规范 |
|---|---|
| 背景 | 纯白 |
| 模块框 | 白色底 + **黑色描边**圆角框 |
| 框内文本 | 第一行 = 模块名（粗体）；其后 = shape 变换流（等宽字体，`a → b`）；**`shape`/`note`/`footnote` 超宽会由 `wrapText()` 自动折行**，不要硬塞成单行溢出框外 |
| 箭头 | 框间**居中**的「线轴 + 箭头头」短竖箭头，从上一框底边指向下一框顶边；**尖端必须紧贴（顶到）下方框顶边，不留空隙**。重复容器内部子模块之间也必须用同样居中箭头，且**间距要足够大**（建议子模块间隙 `subGap ≥ 30px`，否则箭头头会挤成孤立小三角） |
| 重复层 | **虚线框**包裹子模块，右上角「×具体数字」黑底徽标 |
| 图例 | **右上角**固定，说明字母与具体数字的含义 |

**shape 写字母还是写数字（重要原则）**
- **不定值（随输入变化）→ 保留字母**：如 batch size（`N`）、序列长度（`T`/`S`）。
- **模型确定的维度 → 图例里给「具体数字 + 含义」**：如 `4096 = 隐藏维度`、`64 = 路由专家数`、`4 = top_k`。
- 同一字母可能两层含义，要分开标注：如 `N` 作 batch size 与「重复层数」（`×32`）是两回事，需区分。

## 3. 怎么产出新图（最小操作）
1. 把 `template.html` 复制到**当前工作目录**（用户未另指时默认落在此处；用户指定了目录则按其指定）。
2. 修改 `nodes` 数组：
   | 字段 | 说明 | 示例 |
   |---|---|---|
   | `title` | 模块名 | `"多模态编码器 + 投影层"` |
   | `shapes` | shape 变换流数组，每项一行（等宽字体） | `["[N, T, 3072] → [N, T, 4096]"]` |
   | `note` | 可选小字说明 | `"64 路由专家 · top-4 · 共享 1"` |
   | `type:"repeat"` | 标记为重复容器 | `type:"repeat"` |
   | `badge` | 容器右上角重复次数徽标 | `"×32"` |
   | `subs` | 容器内子模块数组，每项同有 `title/shapes/note` | `[{title,shapes}, …]` |
   | `type:"group"` | 嵌套重复组：`subs` 内的一项可再作一个带自身徽标的重复组（用于**块级重复**） | `{type:"group", title, badge:"×n", subs:[…]}` |
   | `type:"branch"` | **多模态并行分支**：文本 / 图像 / 视频走不同列，底部汇合后再进 LLM | `{type:"branch", title, cols:[{title,shapes},{…}], note:"汇合后送入 LLM"}` |
   | `footnote` | 容器/组底部脚注（超宽会**自动折行**，勿手动拆行） | `"深层采用稀疏注意力…"` |

**重复块的两种画法（重要）**
- **整段同构 → 一个重复容器**：把「一层的完整模块」填进 `subs`，容器 `badge` 写重复次数。例：`3×KDA + 1×DSA` 循环 `24` 次 → `subs:[KDA,KDA,KDA,DSA]` + `badge:"×24"`，内部把每一层的结构画出来，再整体乘 ×24。
- **非均匀（头部不同 + 尾部重复）→ 嵌套组**：外层容器是**纯分组**，它的 `subs` 里放若干个 `type:"group"`，每个组各自设 `badge`（×N）。例：`3 层 Dense + 57 层稀疏` → 外层 `"Transformer Decoder 层"`（**不设 `badge`**，标题也不要写「共 60 层」这类重复总数，因为内层组已各自带 ×3 / ×57），内部 `group "Dense 层" ×3` + `group "稀疏层" ×57`，把两类的模块分别画进各自组，避免糅合成一个总框。
3. 更新右上角图例 `.legend` 块（只列本图用到的）；其余 CSS/渲染函数不用动。
4. 整图标题（左对齐避开图例）可用 `line(...)` 调两行标题。

## 4. 生成后校验（必做）
渲染前/后脚本化验证，确认以下几点（用一次 `read` 检查产出文件，或用简单 Node 脚本跑一遍）：
- **坐标合法**：所有框/箭头 x 坐标应等于 `CX`，坐标中不出现 `NaN`、`undefined`。
- **箭头紧贴框**：箭头终点 `y2` 应等于对应框顶边的 `y`；用绘制顺序（先画箭头、后画框）让描边盖住末端最后一像素。
- **箭头样式完整**：是「线轴 + 箭头头」，不是孤立小三角；重复容器内子模块间的箭头与主流程一致，`subGap ≥ 30px`。
- **文字不溢出**：渲染用 `wrapText()` 对 `shape`/`note`/`footnote` 按框宽自动折行，校验时确认没有单行文本冲出框外、没有被硬塞成溢出裁切；`shapes` 仍可手动拆成多行数组（`shapes:[line1,line2]`）或减小字号。
- **图例完整**：图例只含本图用到的符号/数字，`N`/`T` 与「重复层数」等歧义符号已区分，且标注「具体数值随配置不同而有差异」作免责（用在正式文档时改为取自官方 `config.json` 的精确值）。**不要**在图里或图例里标注「来自 config.json / 取自某文件」这类来源或采集说明，保持画面干净，只呈现数值本身。
- **自包含性**：产出是一份独立 HTML，无外部依赖，双击浏览器即可查看。

## 5. 输出
- **默认**：把画好的自包含 HTML 写到**当前工作目录**（新文件，文件名建议带上模型名，如 `llm-compute-graph-DeepSeek-V3.html`），并给出文件路径；若用户明确指定了目录，则写到其指定目录。
- 「当前工作目录」即 agent 运行时的 cwd（例如 DeepSeek Harness 下是 profile 的 workspace 根，Claude Code 下是其当前项目目录）；不确定时先跟用户确认图要放哪。
- 产出为自包含 HTML，可在浏览器直接打开、缩放；若目标环境无法直接预览，可另附渲染说明（如导出 PNG）。

## 6. 复用的正确姿势
渲染引擎写一次不动；模型配置（`nodes` + 图例）每次只改这里。批量复用 = 换一套 `nodes`/图例，排版自动完成。
